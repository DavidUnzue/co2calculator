const config = {
  port: process.env.PORT,
  goclimate_api_root: process.env.GOCLIMATE_API_ROOT,
  goclimate_api_key: process.env.GOCLIMATE_API_KEY,
  auth_token: process.env.AUTH_TOKEN,
};

export default config;
